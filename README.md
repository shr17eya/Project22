# Project22

Intern Technical Assessment submission.

This repo currently holds two proven-working scaffolds — a Python one and a
C# one — set up ahead of the assessment as a dry run. The actual solution
will be built into one of these (most likely `python/`) once the problem
statement is released.

## Python

```bash
cd python
python -m venv .venv
.venv\Scripts\activate        # Windows
pip install -r requirements.txt

python main.py                # run
python -m pytest -v           # test
```

## C# (.NET 10)

```bash
cd csharp
dotnet build
dotnet run --project src/Project22.App
dotnet test
```

## Assumptions

- (fill in once the real problem is solved)

## Known limitations

- (fill in once the real problem is solved)

## AI usage

- (fill in: what was asked of Copilot/Claude, what was accepted/changed, and why)
